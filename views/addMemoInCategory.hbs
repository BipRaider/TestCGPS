<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">


  <title>Document</title>
</head>
<body>

<div class="container-fluid">
  <div class="row">

      <div class="col-md-4 col-md-offset-4">
                  {{> nav}}
        <form action="/add"  method="post"  id="data">

        <div class="form-group ">
            <label for="Category">Category name</label>
            {{!-- <input type="text" name="category" class="form-control " id="Category" placeholder="Note Category" required> --}}
            <select class="form-control" size="1" name="category" required form="data">
                  <option selected disabled>Выберите категорию</option>
                  {{#each categorys}}
                      <option value="{{_id}}">{{category}}</option>
                  {{/each}}
           </select>
        </div>

        <div class="form-group ">
            <label for="title">Title memo</label>
            {{!-- <input type="text" name="title" class="form-control " id="memo" placeholder="Note Category" required> --}}

            <select class="form-control" size="1" name="memo" required form="data">
                    <option selected disabled>Выберите заметку</option>
                    {{#each memos}}
                        <option value="{{id}}">{{memo}}</option>
                    {{/each}}
            </select>
        </div>
          <button type="submit" class="btn btn-default col-sm-12">Submit</button>
        </form>
    </div>

  </div>
</div>
</body>
</html>